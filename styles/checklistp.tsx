import { createTheme, responsiveFontSizes } from "@mui/material";


  
  let checklistp = createTheme({
    typography: {
        fontSize:16,
        

    },
  });

  
export default responsiveFontSizes(checklistp);