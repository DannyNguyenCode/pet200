import React from 'react'

const route = () => {
    // user enters email
    // check if user in database
    // if success, send email containing passcode
    // redirect to passcode route
    // if not success, notify user that email does not exist
  return (
    <div>route</div>
  )
}

export default route