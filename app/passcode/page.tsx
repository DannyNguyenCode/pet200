import React from 'react'

const page = () => {
    // user enters passcode
    // validate passcode is correct
    // validate if expiration date is not past
    // if success redirect to newpassword route
    // if fail notify passcode is invalid
    // display resend passcode
    
  return (
    <div>route</div>
  )
}

export default page